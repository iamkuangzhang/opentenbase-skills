# Cluster Read-Only Session - 130 / 132

时间：2026-06-21-180007
主机：`192.168.244.130`、`192.168.244.132`
执行方式：root SSH 传输命令，通过 `su - opentenbase -c ...` 执行只读命令。

## VERIFIED

- 两台机器均存在：
  - `/data/opentenbase/install/opentenbase_bin_v2.0/bin/opentenbase_ctl`
  - `/data/opentenbase/install/opentenbase_bin_v2.0/bin/pgxc_ctl`
  - `/data/opentenbase/install/opentenbase_bin_v2.0/bin/psql`
- 两台机器均发现 `pgxc_ctl.conf` 候选。
- `pgxc_ctl monitor all` 为只读状态查看，本轮执行成功。
- `pgxc_ctl monitor all` 在 130 和 132 上均显示：
  - `Not running: gtm master`
  - `Not running: coordinator master cn001`
  - `Not running: coordinator master cn002`
  - `Not running: coordinator master cn003`
  - `Not running: datanode master dn001`
  - `Not running: datanode master dn002`
  - `Not running: datanode master dn003`
- `ps -ef` 未发现运行中的 postgres / gtm 集群进程。
- `ss -lntp` 未发现 OpenTenBase CN/DN/GTM 业务端口监听。
- `psql` 连接 `127.0.0.1:30004` 和 `127.0.0.1:30005` 均失败。
- 本轮未执行 start / stop / init / clean / kill / failover / 配置修改 / 数据库写操作。

## DOCUMENTED

- `opentenbase_ctl status --help` 在 130 上可输出 status 用法。
- `opentenbase_ctl` 在 132 上因 `libpqxx.so` 缺失导致 status help 无法直接运行，需要环境变量或库路径进一步确认。

## UNVERIFIED

- 本轮无法验证 `SELECT 1`、`SELECT version()`、`pgxc_node`，因为当前集群状态为 Not running。
- 未验证 `opentenbase_ctl start/stop`、`pgxc_ctl start/stop`。

## 证据

- `dev/evidence/command-output/2026-06-21-180007-cluster-readonly-130-132.json`
